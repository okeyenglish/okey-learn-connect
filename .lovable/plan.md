

# Исправление отображения изображений из GreenAPI (MAX)

## Корневая причина

Проблема состоит из двух частей:

### 1. Временные URL вместо постоянных
Вебхук `max-webhook` сохраняет временный `downloadUrl` от GreenAPI напрямую в `file_url`. Эти ссылки истекают через несколько минут/часов. Для сравнения, вебхук `wpp-webhook` загружает медиа в Supabase Storage (bucket `chat-media`) и сохраняет постоянный публичный URL.

### 2. Отсутствие fileName для изображений
GreenAPI часто не возвращает `fileName` для изображений (только `downloadUrl` и `mimeType`). В `ChatMessage.tsx` (строка 452) условие `fileUrl && fileName` означает, что если `fileName` = `null`, файл вообще не отрисуется -- пользователь видит только текст "📷 Изображение".

### 3. Текст-плейсхолдер не скрывается
Регулярное выражение на строке 439 (`/^\[(imageMessage|...)\]$/`) скрывает текст типа `[imageMessage]`, но MAX-вебхук пишет `🖼️ Изображение` -- этот формат не совпадает, поэтому текст отображается вместе с (отсутствующей) картинкой.

## План исправления

### Часть 1: `supabase/functions/max-webhook/index.ts` -- загрузка медиа в Storage

В функции `extractMessageContent` (строки 334-352):
- При наличии `downloadUrl`, скачать файл и загрузить в bucket `chat-media` (аналогично тому, как делает `wpp-webhook`)
- Сохранить публичный URL из Storage вместо временного GreenAPI URL
- Сгенерировать `fileName` если его нет (например, `image_1707123456.jpg` по MIME-типу)

Так как `extractMessageContent` -- синхронная функция, загрузку в Storage нужно будет вынести в основной обработчик (после вызова `extractMessageContent`), либо сделать функцию async.

Конкретные шаги:
1. Сделать `extractMessageContent` async
2. Принимать `supabase` клиент и `organizationId` как параметры
3. При наличии `downloadUrl` -- скачать через `fetch`, загрузить в `chat-media` bucket
4. Генерировать имя файла на основе mimeType если fileName отсутствует
5. Вернуть постоянный Storage URL вместо временного

### Часть 2: `src/components/crm/ChatMessage.tsx` -- улучшить скрытие плейсхолдера

Строка 439: расширить regex чтобы он также скрывал текстовые плейсхолдеры формата GreenAPI:

```text
Текущий: /^\[(imageMessage|videoMessage|audioMessage|documentMessage)\]$/
Новый:   /^\[(imageMessage|videoMessage|audioMessage|documentMessage)\]$|^🖼️ Изображение$|^🎬 Видео$|^🎵 Аудио$|^📷 Фото$/
```

### Часть 3: `supabase/functions/max-webhook/index.ts` -- fallback для fileName

В `extractMessageContent`, если `fileName` = null, генерировать его из mimeType:

```text
fileName = messageData.fileMessageData?.fileName || generateFileName(fileType, messageData.typeMessage);
```

Где `generateFileName` создает имя вида `image_1707123456.jpg` на основе MIME-типа.

## Технические детали

### Файл 1: `supabase/functions/max-webhook/index.ts`

1. Добавить вспомогательную функцию `uploadMediaToStorage(supabase, orgId, downloadUrl, mimeType)`:
   - `fetch(downloadUrl)` для скачивания
   - Определить расширение из mimeType
   - Загрузить в `chat-media/{orgId}/{timestamp}_{random}.{ext}`
   - Вернуть публичный URL

2. Добавить функцию `generateFileName(mimeType, typeMessage)`:
   - Из mimeType извлечь расширение (image/jpeg -> jpg)
   - Сгенерировать уникальное имя

3. В `extractMessageContent` (строка 339): добавить fallback для fileName

4. В основном обработчике: после `extractMessageContent`, если есть `fileUrl` (временный) -- вызвать `uploadMediaToStorage` и заменить URL

### Файл 2: `src/components/crm/ChatMessage.tsx`

Строка 439: расширить regex для скрытия плейсхолдерных текстов GreenAPI-формата, чтобы при наличии картинки не дублировался текст "Изображение".

## Ожидаемый результат

- Изображения из GreenAPI будут загружаться в Storage и отображаться постоянно (как в WPP)
- Плейсхолдерный текст ("Изображение") будет скрываться если медиафайл прикреплен
- Имя файла всегда будет заполнено, что гарантирует отрисовку компонента `OptimizedAttachedFile`

