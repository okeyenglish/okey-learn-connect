
# Добавить массовую привязку в админ-панель "Филиалы организации"

## Проблема
Компонент `BulkBranchReassign` был добавлен на страницу Настроек (`Settings.tsx`), но пользователь работает через админ-панель (модальное окно), где раздел "Филиалы организации" рендерится в `AdminDashboard.tsx`.

## Что нужно сделать
Добавить `BulkBranchReassign` в раздел `branches` компонента `AdminDashboard.tsx` (строки 127-136), чтобы он отображался под списком филиалов в админ-панели.

## Технические детали
- Файл: `src/components/admin/AdminDashboard.tsx`
- Добавить импорт `BulkBranchReassign` и вставить компонент внутрь case `"branches"` после `<BranchesSettings />`
