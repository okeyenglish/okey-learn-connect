
# План: Исправление формата телефона и отображения мессенджеров

## Проблемы

На основе анализа кода и скриншота выявлены две проблемы:

### 1. Телефон без кода страны
В заголовке чата (слева) телефон отображается как `9670894020` вместо `+7 967 089-40-20`.

**Причина:** В `ChatArea.tsx` (строки 2007, 2109) переменная `clientPhone` выводится без форматирования:
```tsx
<p className="text-sm text-muted-foreground">{clientPhone}</p>
```

### 2. Дублирование информации о мессенджере
В карточке клиента (справа) отображается отдельная строка `WA: +7 (967) 089-40-20`, хотя иконки мессенджеров уже есть напротив номера телефона.

**Причина:** В `FamilyCard.tsx` (строки 520-561) функция `getMessengerInfo()` выводит текст в зависимости от активной вкладки мессенджера.

## Изменения

### Файл 1: `src/components/crm/ChatArea.tsx`

**Что исправить:**
- Добавить импорт `formatPhoneForDisplay` из `@/utils/phoneNormalization`
- Использовать функцию для форматирования телефона в двух местах

| Строка | Было | Станет |
|--------|------|--------|
| 2007 | `{clientPhone}` | `{formatPhoneForDisplay(clientPhone)}` |
| 2109 | `{clientPhone}` | `{formatPhoneForDisplay(clientPhone)}` |

### Файл 2: `src/components/crm/FamilyCard.tsx`

**Что исправить:**
- Удалить блок кода (строки 520-561), который показывает `WA: ...`, `TG: ...`, `MAX: ...` под именем клиента
- Информация о подключённых мессенджерах уже отображается в виде иконок напротив номера телефона в `ContactInfoBlock`

**До изменения (строки 517-562):**
```tsx
<p className="text-sm text-muted-foreground">
  {getRelationshipLabel(activeMember.relationship, activeMember)}
</p>
{/* Show messenger ID based on active tab */}
{(() => {
  const getMessengerInfo = () => {
    if (activeMessengerTab === 'telegram') { ... }
    if (activeMessengerTab === 'whatsapp') { ... }
    if (activeMessengerTab === 'max') { ... }
  };
  ...
})()}
```

**После изменения:**
```tsx
<p className="text-sm text-muted-foreground">
  {getRelationshipLabel(activeMember.relationship, activeMember)}
</p>
{/* Messenger info removed - icons are shown in ContactInfoBlock */}
```

### Файл 3: `src/components/crm/ContactInfoBlock.tsx`

**Проверка:** Функция `formatPhone` (строки 213-229) уже корректно форматирует телефон с кодом страны. Изменения не требуются.

## Результат

После изменений:
- Телефон в заголовке чата: `+7 (967) 089-40-20` вместо `9670894020`
- Под именем клиента: только роль (`Родитель`), без дублирования `WA: +7...`
- Мессенджеры отображаются в виде иконок рядом с номером телефона (уже реализовано в `ContactInfoBlock`)

## Визуальное сравнение

**Было:**
```
Валентина Кулакова #C74816
Родитель
WA: +7 (967) 089-40-20

📞 +7 967 089-40-20  [WA] [TG] [MAX]
```

**Станет:**
```
Валентина Кулакова #C74816
Родитель

📞 +7 967 089-40-20  [WA] [TG] [MAX]
```
